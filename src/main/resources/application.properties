# Database Configuration (uses environment variables with defaults for local development)
spring.datasource.url=jdbc:postgresql://${DB_HOST:db.rhwhtzobrjmjdsahuent.supabase.co}:${DB_PORT:5432}/${DB_NAME:postgres}?sslmode=require
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:Damilola@200501}

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server
server.port=${PORT:8080}

# H2 Console (disabled in production)
spring.h2.console.enabled=false

# Logging
logging.level.org.springframework=${LOG_LEVEL_SPRING:WARN}
logging.level.com.example.qrattendance=${LOG_LEVEL_APP:INFO}
