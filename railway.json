{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x mvnw && ./mvnw clean package -DskipTests",
    "startCommand": "ls -la target/ && java -jar target/qrattendance-0.0.1-SNAPSHOT.jar",
    "run": "ls -la target/ && java -jar target/qrattendance-0.0.1-SNAPSHOT.jar"
  },
  "deploy": {
    "startCommand": "ls -la target/ && java -Xmx512m -Xms256m -jar target/qrattendance-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "checks": {
    "enabled": true,
    "path": "/actuator/health",
    "initialDelay": 60,
    "timeout": 5
  }
}